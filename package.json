{
  "name": "nexus-core",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@10.5.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "npm run test --workspace=@nexus-core/backend",
    "test:e2e": "npm run test:e2e --workspace=@nexus-core/backend"
  },
  "devDependencies": {
    "turbo": "latest"
  }
}
